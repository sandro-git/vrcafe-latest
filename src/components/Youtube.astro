---
import { YouTube } from '@astro-community/astro-embed-youtube';

interface Props {
    videoId: string;
}

const { videoId } = Astro.props;
console.log(videoId);

---

<YouTube id={videoId} class="w-full h-auto rounded-lg" />