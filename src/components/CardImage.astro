---
import { Image } from "astro:assets";

const { src, alt, slug } = Astro.props;
---

<a href={slug}>
  <Image
    loading="lazy"
    class="rounded-t-lg"
    src={src}
    alt={alt}
    format="avif"
  />
</a>
